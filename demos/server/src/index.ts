import { RUNTIME_PACKAGE_NAME } from "@frenchfryai/runtime";

export const SERVER_DEMO_NAME = "@frenchfryai/demo-server";

export const startupMessage = `${SERVER_DEMO_NAME} scaffold is ready via ${RUNTIME_PACKAGE_NAME}`;
